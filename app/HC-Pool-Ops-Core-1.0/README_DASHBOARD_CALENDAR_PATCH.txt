POOL OPS DASHBOARD MONTHLY CALENDAR PATCH

What this does:
- Replaces the dashboard client/property/job card block with a monthly planning calendar.
- Keeps quick buttons for Clients / Properties / Jobs at the top.
- Leaves the data alone. It does not touch your database.

Install:
1. Extract this ZIP.
2. Copy the app folder into:
   C:\Users\Heinl\OneDrive\Desktop\HC-Pool-Ops-FIXED-BY-JARVIS
3. Choose Replace All.
4. Restart server:
   uvicorn app.app:app --host 0.0.0.0 --port 8000 --reload
5. Hard refresh phone Safari.
